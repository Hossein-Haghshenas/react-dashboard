import clsx from "clsx";

const Post = ({ className }) => {
  return (
    <section>
      <section className={clsx("w-[15rem] sm:w-[11rem] h-[10rem] sm:h-[8rem] bg-[#232835] rounded-lg")}></section>
    </section>
  );
};

export default Post;
